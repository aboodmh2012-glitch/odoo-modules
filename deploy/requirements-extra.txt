# Extra Python deps not shipped by odoo:18.0.
# Do NOT list cryptography / lxml / requests / PyPDF2 / reportlab / urllib3 —
# upgrading those breaks the Debian pyOpenSSL stack in the official image.
boto3~=1.40
cssselect~=1.2
defusedxml~=0.7
openpyxl~=3.1
google-auth-oauthlib~=1.2
google-api-python-client~=2.130
